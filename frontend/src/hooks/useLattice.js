// src/hooks/useLattice.js
// This hook is reserved for future lattice-specific state (e.g., node positions, camera transitions)
// Currently, all 3D logic lives inside ThreeScene.jsx and uses the global store.
export default function useLattice() {
  return null;
}
